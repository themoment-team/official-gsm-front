import styled from '@emotion/styled';

export const ListPageH2 = styled.h2`
  ${({ theme }) => theme.typo.h2}
  color: ${({ theme }) => theme.color.gray['090']};
  font-weight: 600;
  margin: 3rem 0 2rem;
`;
