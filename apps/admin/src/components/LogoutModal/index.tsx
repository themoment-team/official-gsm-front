import * as S from './style';

interface ApproveModalProps {
  close: () => void;
  name: string;
}

const LogoutModal: React.FC<ApproveModalProps> = ({ close }) => (
  <S.ModalContainer>adsf</S.ModalContainer>
);

export default LogoutModal;
